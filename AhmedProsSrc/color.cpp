#include "main.h"


// This is a function
void update_screen_with_color(pros::Optical &sensor) {
    double hue = sensor.get_hue();
    double proximity = sensor.get_proximity(); // Checks if an object is actually there
void update_screen_with_color(pros::Optical &sensor) {
	}
}
